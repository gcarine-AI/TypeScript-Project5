
   function calcularVariacaoPercentual(atual, anterior) {
    if (anterior === 0) return 0;
    console.log(typeof despesasTotal);
    console.log(despesasTotal);

    return ((atual - anterior) / anterior) * 100;
}
